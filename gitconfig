[user]
  name = Eric Schmitt
  email = 645839+eschmitt@users.noreply.github.com
[github]
  user = eschmitt
[core]
  editor = vim -f
	excludesfile = /Users/ericschmitt/.dotfiles/gitignore-global
	autocrlf = false
	safecrlf = true
	exludesfile = /home/eric/.dotfiles/gitignore-global
[merge]
  summary = true
  log = true
[alias]
  d = diff
  ds = diff --staged
  l = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short
  la = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short --all
  hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short
  s = status -s -b
  c = commit -v
  st = status
  ci = commit
  co = checkout
  br = branch
  bam = branch --merged origin/master
  a = add -v
  au = add -u
  aa = add -A
  ap = add -u -p
  type = cat-file -t
  dump = cat-file -p
[color]
  diff = auto
  status = auto
  branch = auto
  interactive = auto
  ui = true
  pager = true
[color "branch"]
  current = yellow reverse
  local   = yellow
  remote  = green
[color "diff"]
  meta = blue
  frag = magenta
  old  = red
  new  = green
[color "status"]
  added = yellow
  changed = green
  untracked = blue
